global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: openbank-sandbox
    metrics_path: /metrics
    static_configs:
      - targets:
          - app:3000
